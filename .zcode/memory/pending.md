# Pending

- OPERATOR PICK NEEDED (r012): search-row sizing — Option A (38px field, 16px icons, flush) vs Option B (32px/14px) in docs/mockups/mockup-searchrow.html; then apply to GTK incl. new pause='columns' and stop='stop' icons.
- Roadmap next: filters (type/size/date popover), preview snippet match highlighting, throughput benchmark, .deb packaging.
- Icon tint decision still open: monochrome-white (current) vs accent-tinted active states.

