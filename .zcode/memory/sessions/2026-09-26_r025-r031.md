# Session handoff — 2026-09-26, responses r025–r031

State for the next session. Ledger r-index at r031. Global memory
(linfilesearch-bootstrap-state) is current through r031; changelog.md has
per-response entries; decisions.md now includes r025–r031.

## Where the code is

- HEAD 6fa3c24 pushed to MensuraMedia/linfilesearch (main). 56 tests passing
  (`cd /home/user/projects/linfilesearch && python3 -m pytest tests/ -q`).
- Backups through 20260926-020751 (~/backups/linfilesearch, s009 with -m).
- App runnable: `cd src && python3 main.py` (instance may or may not be
  running; the operator closes it between phases — never assume).

## This session's work

1. r025: operator reported LinShot* missing the last 5 days. Engine proven
   lossless first (370/370 vs find, mtimes 370/370). Two adversarial agents
   then reviewed everything; all P1/P2 findings applied (see decisions.md
   r025). Real UI bug found by test: scope-chip toggle handler was
   re-entrant — clicking a mount chip silently reverted to All.
2. r026/r027/r030/r031: sidebar brand iterations — filled 132px mark,
   caption raised in two 7px steps via SVG bottom trim (LOGO_MARK_BOTTOM_TRIM
   = 12; GTK has no negative margins — pixbuf crop is the mechanism), nav
   buttons follow.
3. r028/r030: preview rail replaced by a 28×28 square caret toggle at the
   top of a transparent strip; verified pixel-flush with the header band
   (both edges y180–207) — do NOT nudge it without new evidence.

## Operator patterns that matter

- Pixel instructions are exact (up 7, up 4, +2): implement via measurement,
  then verify allocations before claiming.
- Annotated screenshots may contain red markup; verify the markup against
   real geometry before 'fixing' what it marks (r031: red line was 3px high).
- Operator closes the app between phases; relaunch for verification, never
  assume it is running. No input automation exists on this machine.
- Mockup-first for aesthetic changes (docs/mockups/, served on 127.0.0.1:8765).

## Open threads

- See .zcode/memory/pending.md (filters popover, benchmark, .deb, icon tint,
  hover real-mouse check, fractional-scaling caveat).
