# Changelog — linfilesearch

Append-only. Newest entries at the bottom. One entry per completed change.

## 2026-09-25
- Project bootstrapped with development standards (s003_a).
- Vendored gtk-python-dashboard-starter into src/, resources/, run.sh (docs/starter-README.md kept; demo gif excluded); live-launched and operator-tested on X11.
- Phosphor icon library downloaded to ~/projects/assets/icons via s011_c (1512 icons × 6 weights, MIT).
- Icon subset installed to resources/icons/{regular,fill} via s012_a (53 icons × 2, manifest.txt).
- Technical concept written: docs/file-search-concept.md (features, mountpoint handling, engine, icon system, roadmap).
- UI mockups A/B/C + review index built in docs/mockups/ with real icons; screenshots archived (mockup-a/b/c.png); all three verified defect-free by image analysis; index.html presents them with the live-starter baseline.
- Operator selected direction: A + collapsible right preview pane (r004). Mockup A2 built as interactive fold-out (eye tool / rail / caret toggle; #collapsed URL preset); both states rendered and verified; column-clipping and edge-spacing defects found by image analysis and fixed; review index updated with A+ card. IAB screenshot capability broke mid-session; captures moved to headless Firefox.
- Git: design phase committed (b6bb7df) and pushed to private MensuraMedia/linfilesearch (operator instruction, r005); plugin cache dirs added to .gitignore; secret scan clean.
- Build v0.1 (r005): manager_mounts (proc-mounts parsing + lsblk enrichment + Gio.UnixMountMonitor, verified: /, /home, /mnt/data searchable; /boot/efi and non-gvfs FUSE excluded), manager_search (streaming per-mount workers, pause/stop, fnmatch full-match wildcards, guarded regex), icon_loader (Pillow alpha-mask tinting, cached pixbufs), page_search implementing the A+ layout with Gtk.Revealer fold-out preview, history/saved stubs, sidebar+content rewiring, search CSS, 13 unit tests passing. App live-verified against the mockup design (screenshot docs/mockups/app-live.png).
- Button cap rule (r006): main-page buttons never exceed sidebar nav dimensions (28px tall × 130px wide). Caps centralized in config_layout (MAIN_BUTTON_*), button icons reduced to 14px, preview actions restyled horizontal, CSS min-height caps + explicit label font rules (starter's `* {font-size:11pt}` directly matches child labels and beats inheritance), TreeView header buttons capped. New enforcement test builds the real window and measures every content-area button — it exposed that resources/css/style.css was never loaded at startup (only inline theme colors); now loaded in main.py. 14 tests passing. Live screenshot docs/mockups/app-live-r006.png.
- History & saved searches (r007): manager_history persists history (capped 100) + saved searches as JSON under ~/.config/linfilesearch/. SearchPage records every run (query, mode, case, scope) and patches final match count/elapsed. History page: spreadsheet view (Time/Query/Mode/Case/Scope/Matches), single-click re-runs via AppController (switches to Search page, sidebar synced, all criteria prepopulated, search auto-starts), per-row bookmark column files the search under Saved (accent-tinted when saved), Clear button empties history. Saved page: same sheet with trash column to remove. Results columns drag-reorderable (set_reorderable). Stop button verified wired (tooltip 'Stop search'; safe no-op idle). 27 tests passing.
- Button size reduction + license (r008): operator flagged main-page buttons still too large. Root cause: preferred-height measurements were not predictive of rendered size. Fix: every main-page button now pinned to exactly 24px via height-request (config MAIN_BUTTON_TARGET_HEIGHT), 12px icons, 8pt labels, tightened CSS (padding 1-2px, min-height 18-20px). Pixel-verified from a live screenshot: main Search button 24px vs sidebar nav button ~40px at 1:1 scale. License added: CC BY-NC 4.0 (LICENSE with author header, canonical legal code; author MensuraMedia; free to use/copy/modify/distribute, commercial use prohibited without permission) + README.md with license section. 27 tests passing.
- Installer, tray, app icon, README (r010): app icon set generated (s013 — dark rounded tile + blue Phosphor magnifier, 13 sizes 16-512 + refreshed sidebar logo); user-level installer (s014 — hicolor icons, ~/.local/bin/linfilesearch wrapper, .desktop with StartupWMClass; --uninstall). TrayManager (modules/manager_tray.py) via AyatanaAppIndicator3 with Gtk.StatusIcon fallback: Show/Hide, New Search, About, Quit; closing the window hides to tray. Window carries the app icon list for taskbar/ALT+Tab; WM_CLASS verified 'linfilesearch'; panel verification confirms the icon in both the window-list button and the system tray. Search-row polish per operator screenshot: mode toggles joined into one segmented strip, pause/stop borderless, chips roomier, preview head/actions alignment. README: real screenshot, Installation section, compatibility matrix (verified Mint 22.3; expected Ubuntu/Debian/Fedora/Arch/openSUSE; Wayland notes; tray dependency). 27 tests passing.
- Spreadsheet behavior, real previews, uniform row, save fix (r011): double-clicking a column boundary auto-fits the column to its contents (treeview_utils, applied to results/history/saved; capped 400px); vertical grid lines + visible header cell borders on all sheets. Preview pane now shows real content: image thumbnails (GdkPixbuf), PDF first page (pdftoppm), odt/docx/xlsx text extracted from the zip XML, plain text as before (utils/preview.py). Sidebar logo regenerated with clear transparent background (s013_a — magnifier only, no tile). Search row unified: entry, mode strip, pause/stop, and Search button all pinned to SEARCH_ROW_HEIGHT (30px); MAIN_BUTTON_MAX_HEIGHT corrected to 36 (the rendered sidebar size — nominal 28 was never what rendered). Fixed the real save bug: History/Saved click handlers referenced Gdk.BUTTON_PRIMARY_BUTTON which does not exist in the GDK3 typelib — every click raised AttributeError; now compares button==1, verified with a realized-view click test (bookmark files to Saved, row click re-runs). 33 tests passing.
- Search row sizing study (r012): operator rejected r011 row proportions and requested a preview-first mockup referencing the original A2 proportions. Icons added to subset (s012_b): plain 'stop' square for the stop control, 'columns' (two bars) for pause. docs/mockups/mockup-searchrow.html presents three dimensioned candidates — Option A (original-mockup proportions: 38px field, 16px icons, all controls flush to field height), Option B (compact 32px/14px), and the current GTK row for contrast — verified render-clean; opened in the operator's browser. GTK application deferred until the operator picks. Review index updated.
- Option A applied (r013): operator chose Option A; spec documented in docs/search-row-spec.md. App changes: SEARCH_ROW_HEIGHT=38 with all row controls flush (mode toggles at 36 inside the 2px-bordered strip), SEARCH_ROW_ICON_SIZE=16 for row icons, ICONS pause='columns' stop='stop', MAIN_BUTTON_MAX_HEIGHT=40 (rendered sidebar ceiling). Two GTK3 sizing traps fixed along the way: the entry's natural height exceeded its pin (theme entry padding — capped via CSS min-height/padding), and widget margins are double-counted in natural height (moved to CSS padding). Pixel-verified live: accent band of the row measures exactly 38px, toolbar 24px; visual pass confirms flush strip, distinct two-bar/square icons, no regressions. 33 tests passing.
- UI polish (r015): sidebar logo compacted per original-mockup reference — 56px transparent mark + 'LINFILESEARCH' wordmark below (9pt, 1.5px letterspacing, #bbb), replacing the 145px fill. Pause/stop controls now carry thin borders (#1a1a1a). Stop control tints RED (#d9534f) while a search is running and reverts on stop/finish (set_stop_running; unit-tested state swap). All spreadsheet header cells (results/history/saved) now have a clear full border in #ccc per operator spec. 35 tests passing; live screenshot verified (docs/mockups/app-live-r015.png).
- Spreadsheet and layout fixes (r016): Path-column auto-fit fixed (edge detection now uses exact header-button allocations; autofit cap raised to 1000px so long paths genuinely expand — operator reported Path margin did nothing while Name worked). Selected row keeps its blue under cursor hover (row:selected:hover CSS). #ccc header borders removed — replaced with subtle #4a4a4a pending the header-border study (docs/mockups/mockup-headers.html: subtle/hairline/accent-rule/raised-band variants, opened in operator browser). Header cell padding now ≥4px all sides. History/Saved columns set to fixed widths per operator reference (Time 130 / Query 420 / Mode 150 / Case 60 / Scope 280 / Matches 70; trash 52, mark 44). Sidebar mark+wordmark group vertically centered as a unit. About page rewritten as a linfilesearch page (was starter 'About GTK' filler) with margins matching the other pages. Progress line moved above the results, border removed, all status labels uniform 9pt, evenly spaced between chips row and spreadsheet. 37 tests passing; live screenshot verified (docs/mockups/app-live-r016.png).
- Header style V2 applied (r017): operator chose V2 Soft Hairline from the study; spec documented in docs/header-style-spec.md (1px #767676 full border, #313131 fill, #aaa 9pt text, 4/6px padding). Applied to all three sheets via shared CSS; study and review index marked chosen+applied. Pixel-verified (1,833 #767676 pixels in the header band) and visually confirmed uniform with no regressions. 37 tests passing; screenshot docs/mockups/app-live-r017.png.
- Layout refinements (r018): sidebar mark repositioned per reference (upper-center icon, caption anchored near the bottom — pack_end with 8px bottom margin, 20px top offset). Status line empty at idle ('Ready.' removed). Header cell borders de-doubled: each cell draws top/left/bottom only, last cell closes the right edge — shared edges render as a single hairline (operator reported doubled borders; GTK3 header buttons don't border-collapse). Preview pane header removed (eye icon + 'Preview' + X); replaced by a slim always-visible rail on the pane's left edge whose caret flips caret-right (open) / caret-left (closed); the eye toolbar button was removed as redundant; caret-left added to the icon subset (s012_c). Pause + Stop joined into one connected control (ps-group) matching the mode strip. 37 tests passing; live screenshot verified (docs/mockups/app-live-r018.png).
- Brand mark study (r019): operator requested the upper-left logo become list-magnifying-glass as a large, well-centered thumbnail, mockups first. Icon added to subset (s012_d); docs/mockups/mockup-logo.html presents four treatments in the real 150×150 area — V1 56px regular, V2 72px regular (large thumbnail), V3 72px fill, V4 64px regular with 6px optical right-nudge. Render verified clean; opened in operator browser; GTK application deferred to operator pick.
